<h1>Good deals nearby and for today</h1>
  <div class="container">
    <div class="row">
    <div class="text-center" class ="col-xs-12 col-sm-6 col-md-4" class="cocktail-div">
      <% @promotions.each do |promo| %>
            <h3> <%= link_to promo.title, promotion_path(promo)%> proposed by <%= promo.seller.username %> </h3>
              <strong> <%= promo.producttype %> </strong> - <%= promo.description %>
                <%= promo.priceperunit %> € --> <%= promo.discount%> % discount
                  valid until : <%= promo.duration %>
                    Avalaible quantity : <%= promo.remainingquantity %> <%= promo.unit%>
          <% end %>
      </div>
   </div>
 </div>

    <!-- Include Google Maps JS library. You should use your *own* key -->
    <script src="https://maps.google.com/maps/api/js?key=AIzaSyDANjx3bosEtIyzJaoWs50Wnt6nt_1rmxU"></script>

    <!-- Include Dependencies of GMaps for Rails gem -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/underscore.js/1.8.3/underscore-min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/js-marker-clusterer/1.0.0/markerclusterer_compiled.js"></script>
    <script src="https://cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js"></script>
    <script src="https://cdn.rawgit.com/apneadiving/Google-Maps-for-Rails/master/js_compilation/gmaps_google.js"></script>

<div id="map" style="width: 100%; height: 600px;"></div>
 <% content_for(:after_js) do %>
  <%= javascript_tag do %>
    $(document).ready(function() {
      var handler = Gmaps.build('Google');
      handler.buildMap({ internal: { id: 'map' } }, function() {
        markers = handler.addMarkers(<%= raw @hash.to_json %>);
        handler.bounds.extendWith(markers);
        handler.fitMapToBounds();
        });
    });
    <% end %>
  <% end %>

<%= link_to 'Edit'  %>
<%= link_to 'New' %>
<p>Find me in app/views/promotions/index.html.erb</p>
